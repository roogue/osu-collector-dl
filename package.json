{
  "name": "osu-collector-dl",
  "version": "2.0.0",
  "main": "./dist/src/index.js",
  "scripts": {
    "build": "tsc -p .",
    "start": "yarn run build && node .",
    "test": "yarn run build && node ./dist/src/__test/test.js"
  },
  "license": "MIT",
  "dependencies": {
    "axios": "^0.26.1",
    "dotenv": "^16.0.0",
    "node-osu": "^2.2.1",
    "puppeteer": "^13.5.2"
  }
}
